export const SINGLE_ALPHANUMERIC_CHAR_REGEX = /^[a-zA-Z0-9]$/;

export enum OperatingSystem {
  mac = 'mac',
  windows = 'windows',
  other = 'other',
}

export enum KeyboardKey {
  CTRL = 'CTRL',
  ALT = 'ALT',
  CMD = 'CMD',
}
